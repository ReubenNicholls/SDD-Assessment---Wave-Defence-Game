using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyFollowPlayer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // get what the player is 
    }

    // Update is called once per frame
    void Update()
    {
        /* 
         * locate player
         * turn to player
         * move towards player
         */
    }
}
